## 개선 및 추천 기능 (메뉴 / 아이콘 패널)

### 퀵 액세스 툴바 (Quick Access Toolbar)
- 아이콘 순서 드래그 재정렬 — 현재 추가만 가능하며 순서 변경 불가
- 아이콘 사이 구분선(separator) 추가 기능 — 관련 아이콘끼리 시각적 그룹화
- 아이콘 크기 토글 (소형/기본) — 모니터 해상도·화면 크기에 맞게 밀도 조정
- 드래그 삽입 위치 미리보기 — 메뉴 아이템을 퀵바로 드래그 시 삽입 위치 인디케이터 표시
- 퀵바 구성 프리셋 저장/불러오기 — 업무 유형별(설계/검토/발표) 버튼 세트 전환
- 퀵바 커스텀 버튼 우클릭 편집 — 현재 호버 삭제 버튼 외에 아이콘·이름 변경 기능

### 메뉴바
- 최근 사용 항목 (MRU) — 파일 메뉴 상단에 최근 열거나 내보낸 항목 5개 고정 표시
- 메뉴 전체 검색 — 기능명 또는 단축키로 메뉴 항목을 검색해 바로 실행 (현재 도구>검색은 엔티티 대상)
- 단축키 커스터마이징 — 단축키 목록 화면에서 사용자가 직접 키 재할당
- 메뉴 항목 숨기기 — 자주 쓰지 않는 항목을 숨겨 메뉴를 간소화하고 우클릭으로 복원

### 우클릭 컨텍스트 메뉴 확장
- 엔티티 우클릭: 헤더 색상 변경, 엔티티 복제, 관련 엔티티만 선택
- 관계선 우클릭: 선 스타일 변경(실선/점선), 레이블 편집, 방향 반전
- 캔버스 빈 공간 우클릭: 붙여넣기, 전체 선택, 자동 배치 바로가기

### 패널 및 레이아웃
- 미니맵 패널 — 우측 하단에 전체 다이어그램 조감도 및 현재 뷰포트 위치 표시·클릭 이동
- 속성 패널 (우측 사이드바) — 엔티티나 관계 선택 시 우측에서 속성을 인라인으로 바로 편집
- 엔티티 트리 패널 폭 드래그 조절 — 현재 고정 폭을 마우스로 자유롭게 변경
- 상태 표시줄 (하단 고정) — 선택 항목 수·현재 커서 좌표·줌 레벨·엔티티/관계 총 개수 표시
- 다이어그램 탭 개선 — 탭 색상 지정, 드래그로 순서 변경, 탭 이름 더블클릭 인라인 편집

### 선 겹침 해소 개선 
선 겹침 해소가 선의 겹침 뿐 아니라 엔티티를 가로지르지 않도록 변경 